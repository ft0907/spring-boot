server:
  port: 8080
  context-path: /
  
spring:
  datasource:
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/shopxx-boot?serverTimezone=UTC&characterEncoding=UTF-8
    
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update

  thymeleaf:
    suffix: .ftl
    content-type: text/html; charset=utf-8
    cache: false
    encoding: UTF-8
